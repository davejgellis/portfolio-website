backend:
  name: github
  repo: davejgellis/portfolio-website
  branch: main
  auth_type: pkce
  app_id: YOUR-GITHUB-OAUTH-CLIENT-ID
```

Replace `YOUR-GITHUB-OAUTH-CLIENT-ID` with the Client ID from your GitHub OAuth app.

Also make sure the GitHub OAuth app's **Authorization callback URL** is set to exactly:
```
https://www.daveellis.co.uk/admin/